# Gesture Volume Control

A Python script that lets you control your system's volume using hand gestures via your webcam.

## Requirements

* `opencv-python`
* `mediapipe`
* `numpy`
* `pycaw`

## Installation

Install the required packages using pip:
```bash
pip install opencv-python mediapipe numpy pycaw
